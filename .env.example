# Server
PORT=4000
DATA_DIR=./data

# Admin web UI credentials (WAJIB diisi untuk production)
ADMIN_USER=admin
ADMIN_PASSWORD=ganti-ini-dengan-password-kuat

# JWT session secret (opsional - auto-generate & persist jika kosong)
SESSION_SECRET=